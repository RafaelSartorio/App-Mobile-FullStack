import React from "react";
import { NativeBaseProvider, Box, Button, Center, FormControl, Input } from "native-base";
import Home from './src/screens/navigation/MainContainer';

export default function App() {
  return (
    //Assim que iniciar o App estou chamando a função Main Container.
    <MainContainer/>
  );

}

